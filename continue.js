//continue mane specific item skip korbe

/* var numbers = [45, 32, 46, 89, 68, 30, 75];
for (var i = 0; i < numbers.length; i++) {
    var number = numbers[i];
    if (number > 50) {
        continue;        
    }
    console.log(number);
} */

    var items = ['bottle', 'mobile', 'laptop', 'key', 'mouse', 'keyboard'];
    for (var i = 0; i < items.length; i++) {
        var item = items[i];
        if (item = 'key') {
            continue;
        }
        console.log(item);
    }